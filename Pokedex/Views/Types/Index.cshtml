@using Application.ViewModels.Type
@model List<TypeViewModel>
@{
    ViewData["Title"] = "Tipos";
}


<div class="row mb-3">
    <h2 class="col-3">
        Tipos
    </h2>
    <div class="col-3 offset-6">
        <a class="btn btn-outline-success" asp-controller="Types" asp-action="Create">Añadir Tipo</a>
    </div>
</div>

<div class="row row-cols-1 row-cols-md-2 g-4">

    @foreach(var item in Model){
      <div class="col">
         <div class="card">
          <div class="card-body">
            <h5 class="card-title">@item.Name</h5>
            <a asp-controller="Types" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Editar</a>
            <a asp-controller="Types" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Eliminar</a>
          </div>
        </div>
      </div>
    }


</div>